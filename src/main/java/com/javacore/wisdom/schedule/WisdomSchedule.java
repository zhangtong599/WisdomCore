package com.javacore.wisdom.schedule;

import org.springframework.stereotype.Component;

@Component
public class WisdomSchedule {
/*
	public final static long SECOND = 1 * 100;

	@Autowired
	private NetProperties netpt;

	@Autowired
	private NodeProperties nodpt;

	@Autowired
	private DataProperties datapt;

	@Autowired
	private CheckProperties checkpt;

	@Scheduled(fixedDelay = SECOND)
	public void InitLoadConfigCache() {

		netpt.LoadMapNetProperties();
		nodpt.LoadMapNodeProperties();
		datapt.LoadMapDataProperties();
		checkpt.LoadMapCheckProperties();

	}
*/
}
